# Desktop Commander (optional)
#
# This project uses the GitHub Copilot CLI (`copilot`) via the Copilot SDK.
# In most setups you do NOT need an API key here.

# Optional: explicit path to the Copilot CLI executable.
# If unset, the app will try to find the CLI automatically (or fall back to `copilot` on PATH).
# COPILOT_CLI_PATH=C:\\Path\\To\\copilot.exe

NODE_ENV=development
